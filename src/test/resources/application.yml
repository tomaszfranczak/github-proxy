

server:
  error:
    include-message: always

github:
  reposUrl: https://test.test/users/%s/repos
  branchesUrl: https://test.test/repos/%s/%s/branches

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher